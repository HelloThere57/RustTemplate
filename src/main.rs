// #![allow(unused)] // For beginning only.

use crate::prelude::*;

mod error;
mod prelude;
mod utils;

fn main() -> std::result::Result<(), Box<dyn std::error::Error>> {
	println!("Hello, world!");

	Ok(())
}
